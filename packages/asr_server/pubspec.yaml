name: asr_server
description: HTTP server and client for asr, with a built-in minimal web UI.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  asr: ^0.1.0
  mime: ^2.0.0

dev_dependencies:
  archive: ^4.0.0
  lints: ^5.0.0
  test: ^1.25.0
