name: asr_cli
description: asr 命令行：transcribe / models / serve。
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  args: ^2.6.0
  asr: ^0.1.0
  asr_server: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

executables:
  asr: asr
