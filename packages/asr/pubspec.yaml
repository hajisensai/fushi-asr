name: fushi_asr
description: Speech recognition to subtitles - core plus ONNX runtime backend and subtitle formats.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  fushi_asr_core: ^0.1.0
  fushi_asr_align: ^0.1.0
  fushi_asr_subtitles: ^0.1.0
  fushi_asr_onnx_ffi: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
