name: asr_core
description: Pure Dart speech recognition core - VAD, fbank, zipformer RNN-T and CTC decoding, batching, subtitle output.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  crypto: ^3.0.3
  meta: ^1.15.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
